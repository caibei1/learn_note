如何在项目中应用kafka
1.削峰填谷
如何应对突刺问题，在框架层支持接口异步（kafka），控速消费，通过sync事件同步返回结果。
活动高峰期，数据上报到kafka，控速消费。
2.业务做失败重试，重试指数退避机制。一般是系统错误做重试，业务错误不做重试。
3.重试多次后还是失败可以选择存入mysql，方便后续分析
4.服务之间做弱依赖（可以在框架层做）
调用方先调用rpc接口，失败后再把请求丢入kafka。服务方可以通过消费kafka来重试请求。注意需要做好退避机制
5.kafka挂了可以对kafka做弱依赖，send失败后写入mysql
